{
  "name": "streamed-pk-hls-stream-resolver",
  "description": "Node.js resolver for streamed.pk and embed.st HLS streams: protobuf /fetch handshake, GOAT lock.wasm decrypt in a worker thread, curl relay for strmd.st playlists and PNG-wrapped MPEG-TS segments.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "big-integer": "^1.6.52",
    "happy-dom": "^20.0.10"
  }
}
